Interested in seeing what a rolling 10 game average of powerplay percentage looks like for an OHL team.  This script enables you to change a team and run their regular season and playoff games.  Produces a plot.  Example shown.

teamCodes.csv is a file with team code names and team_id numbers.  These variables are required to parse the api call that pulls from the game summary.
